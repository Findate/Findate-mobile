import 'package:flutter/material.dart';

class NearByDatesScreen extends StatelessWidget {
  const NearByDatesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
